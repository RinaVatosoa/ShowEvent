/**
 * lister le lieu
 */
$('.rv-reserver').click(function () {
    const list_lieu = $('.details_lieu');
    for ( let i = 0 ; i < list_lieu.length; i++){
        const a= list_lieu[i].innerHTML;
    console.log("Nouvelle place réservé" + a);

    }
    alert( 'Nouvelle place réservée' );
})


solde = 0;

if( date_enc === date_dec ) {
    benefice = enc - dec;
}

solde += benefice;

